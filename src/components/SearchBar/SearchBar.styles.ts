import styled from 'styled-components';

// eslint-disable-next-line import/prefer-default-export
export const StyledInput = styled.input`
  position: absolute;
  width: 457px;
  height: 48px;
  left: 459px;
  top: 81px;

  background: #FFFFFF;
  box-shadow: 10px 5px 18px rgba(154, 154, 154, 0.12);
  border-radius: 100px;
`;
